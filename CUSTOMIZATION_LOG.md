# سجل التخصيص — توليب بلوم

**المصدر:** `store-core`
**الهدف:** متجر ورود وهدايا تجريبي مستقل — الرياض، السعودية
**حالة البناء:** `npm run build` ينجح · ٣٥ صفحة تُولَّد · صفر أخطاء

---

# ١. الخلاصة في سطر

٢٥ ملفًا عُدّل، خمسة أُضيفت، عشر صور حُذفت. **١٣١ من ١٥٦ ملفًا بقيت كما هي حرفيًا** — بما فيها `schema.sql` و`middleware.js` و`queries.js` و`cache.js` و`auth.js` و`analytics.js` و`attribution.js` وكل مسارات API وكل لوحة التحكم.

| الفئة | العدد |
|---|---|
| ملفات عُدّلت (تخصيص) | ٥ |
| ملفات عُدّلت (**إصلاح خلل نواة**) | ١٩ |
| ملفات عُدّلت (بنية المشروع) | ١ |
| ملفات أُضيفت | ٥ |
| ملفات حُذفت | ١٠ |
| **ملفات نواة لم تُلمس** | **١٣١** |

> **الجزء الأكبر من التعديلات ليس تخصيصًا لتوليب بلوم.** تسعة عشر ملفًا عُدّلت لإصلاح أخطاء في النواة نفسها — أخطاء كانت ستضرب كل متجر قادم. كلها موثّقة في §٣ وقابلة للرفع إلى النواة الأم كما هي.

---

# ٢. ما عُدّل — تخصيص

## `config/store.config.js` — أُعيدت كتابته

| الحقل | القيمة |
|---|---|
| `name` | توليب بلوم \| ورود وهدايا |
| `tagline` | ورد يُقال به ما لا يُقال |
| `whatsapp` | `966500000000` ⚠️ **رقم افتراضي مؤقّت** |
| `phone` | `+966 50 000 0000` ⚠️ **مؤقّت** |
| `address` | الرياض، المملكة العربية السعودية ⚠️ **يحتاج الحي والشارع** |
| `mapQuery` | `24.7136, 46.6753` ⚠️ **إحداثيات وسط الرياض، لا المعرض** |
| `workingHours` | يوميًا، 9 ص – 11 م |
| `freeShippingThreshold` | `300` (كان 500) — الورد سلة أصغر قيمة |
| `googleProductCategory` | `Home & Garden > Decor > Home Fragrances > Flowers` |
| `keywords` | ١٠ كلمات مستهدفة لسوق الرياض |
| `ticker` | ٤ رسائل |

**الوحدات:** `maintenance` أُطفئت (لا معنى لطلب فني في متجر ورود). البقية كما هي.

## `config/theme.config.js` — إضافة + تفعيل

أُضيف مفتاح سادس `tulip` إلى `THEMES`. **الخمسة الأصلية لم تُمسّ.**

| | |
|---|---|
| `primary` / `primaryDeep` | `#2E2A2B` / `#1A1718` فحمي دافئ |
| `accent` / `accentAlt` | `#C4707F` / `#D89AA5` وردي مغبّر |
| `soft` / `softTint` | `#F0D9DD` / `#FBF3F2` |
| `surface` / `surfaceAlt` / `surfaceMuted` | `#FFFFFF` / `#FAF6F4` / `#FDFBFA` |
| `ink` / `muted` / `mutedLight` | `#241F20` / `#6B5F61` / `#9A8D8F` |
| `line` / `lineSoft` | `#EDE3E1` / `#F6EFEE` |
| `danger` / `success` / `warning` / `gold` | `#C0392B` / `#4F7A5A` / `#D19A3E` / `#C2A15A` |

تغييرات أخرى:

- `ACTIVE_THEME`: `"aqua"` ← `"tulip"`
- `themeColors()` الاحتياطي: `THEMES.aqua` ← `THEMES.tulip`
- `SHAPES.buttonRadius`: `999px` ← `0.75rem` (مستطيل ناعم بدل الكبسولة)
- `SHAPES.radius` / `cardRadius` / `imageRadius`: `1rem` ← `0.875rem`
- `SHAPES.radiusLg`: `1.5rem` ← `1.25rem`
- `TYPOGRAPHY.headingWeight`: `800` ← `700`
- `TYPOGRAPHY.headingLineHeight`: `1.3` ← `1.35`
- `MOTION.duration`: `0.28s` ← `0.32s`

الخط بقي `IBM Plex Sans Arabic`.

## `config/content.config.js` — أُعيدت كتابته

**كل التصديرات الأحد عشر الأصلية موجودة بأسمائها** — القيم فقط تغيّرت. لم يُحذف ولا يُعاد تسمية أي تصدير، فلا يكسر أي استيراد في النواة.

| التصدير | ما صار |
|---|---|
| `FEATURES` | ٤ مميزات: توصيل نفس اليوم · طزاجة · تنسيق يدوي · مساعدة في الاختيار |
| `TRUST_ITEMS` | ٦ عناصر |
| `WHY_US` | ٤ أسباب |
| `HOME_FAQS` | ٤ أسئلة |
| `ALL_FAQS` | ١٠ أسئلة (شحن خارج الرياض · العناية بالورد · الإهداء المجهول · الاسترجاع) |
| `PRODUCT_FAQS` | ٤ أسئلة |
| `CTA` | «قل لنا المناسبة ونرشّح لك» |
| `TESTIMONIALS` | `[]` — بقيت فارغة عمدًا. القسم يختفي تلقائيًا |
| `ABOUT` | فقرتان + رسالة + رؤية + ٤ قيم |
| `BADGE_PRESETS` | ١٤ شارة بألوان الثيم الجديد |
| `SERVICE_OPTIONS` | عُمّمت — الوحدة مطفأة والثابت محفوظ فقط لأن النواة تستورده |

**أربعة تصديرات مضافة:** `SIZE_TIERS` · `SIZE_NOTE` · `OCCASIONS` · `ORDER_STEPS`

## `components/sections/registry.jsx` — إضافة فقط

**الأقسام الأحد عشر الأصلية لم يُمسّ منها سطر.** أُضيف:

- سطران في الاستيراد: `buildWhatsAppLink` و`getIcon` وأربعة ثوابت من `content.config.js`
- ثلاثة مفاتيح جديدة في `SECTIONS`:

| المفتاح | الوصف |
|---|---|
| `sizeGuide` | جدول مقارنة المقاسات — بطاقات على الجوال، جدول على الشاشات الأكبر |
| `occasions` | شبكة ٦ مناسبات، كل واحدة تفتح واتساب برسالة مُعبّأة |
| `orderSteps` | ٤ خطوات لرحلة الطلب، تقرأ من التهيئة |

## `config/sections.config.js` — أُعيدت كتابته

ترتيب الرئيسية الجديد:

```
hero → features → occasions ⭐ → categories → productGrid(bestSellers)
→ sizeGuide ⭐ → productGrid(offers) → orderSteps ⭐ → whyUs
→ trust → testimonials → faq → cta
```

المنطق: المناسبة قبل النوع (الزائر يفكّر «تخرّج» لا «باقات»)، ودليل المقاسات قبل العروض ليزيل التردّد قبل عرض السعر المخفَّض.

`SHOP_SECTIONS` أُضيف إليها `sizeGuide` — **لكنها لا تُستهلك، انظر §٥.٣**.

---

# ٣. ما عُدّل — إصلاح أخطاء النواة

هذه **ليست تخصيصًا**. تسعة عشر ملفًا فيها أخطاء حقيقية في النواة، بعضها يوقف البناء وبعضها يسرّب نصوص متجر سابق إلى كل متجر جديد. **ارفعها إلى النواة الأم كما هي.**

## ٣.١ صفوف `node:sqlite` بلا نموذج أولي — **كان يوقف البناء و/admin/orders**

`node:sqlite` يُعيد صفوفًا بنموذج أولي فارغ (`null prototype`)، وReact Server Components ترفض تمريرها إلى أي مكوّن عميل:

```
Only plain objects, and a few built-ins, can be passed to
Client Components from Server Components.
```

الأعراض: فشل البناء عند وجود أي بنر (`HeroBanners` مكوّن عميل)، و**500 في `/admin/orders`** (`OrdersBoard` مكوّن عميل).

`getProducts` و`getCategories` كانتا تنشران الصفوف يدويًا (`...r`) فنجتا بالصدفة. عشر دوال أخرى لم تفعل: `getOrders` · `getBanners` · `getCategoryBySlug` · `createCategory` · `updateCategory` · `createOrder` · `updateOrderStatus` · `getLegalPages` · `getAllLegalPages` · `getLegalPage`.

**العلاج عند المصدر لا في كل دالة:** غُلّفت `db.prepare` في `lib/db.sqlite.js` فتُسوَّى كل النتائج تلقائيًا، وأي دالة تُضاف مستقبلًا تكون سليمة بلا أن يتذكّر أحد القاعدة.

```js
const plain = (v) => (v && typeof v === "object" ? { ...v } : v);

function withPlainRows(db) {
  const nativePrepare = db.prepare.bind(db);
  db.prepare = (sql) => {
    const stmt = nativePrepare(sql);
    return {
      all: (...a) => stmt.all(...a).map(plain),
      get: (...a) => plain(stmt.get(...a)),
      run: (...a) => stmt.run(...a),
      iterate: (...a) => stmt.iterate?.(...a),
    };
  };
  return db;
}
```

Postgres عبر `pg` غير متأثر — يُعيد كائنات عادية أصلًا.

## ٣.٢ `createBanner` و`updateBanner` — عدد قيم خاطئ

`createBanner` تعلن ١٢ عمودًا وتربط ٩ قيم (`ratio` و`ctaLabel` و`ctaHref` مفقودة). `updateBanner` تعلن ١١ وتربط ٨. النتيجة: **إنشاء أو تعديل أي بنر من لوحة التحكم يفشل على SQLite.** أُصلحت الاثنتان وأُضيفت القيم الناقصة.

## ٣.٣ ‏١٢ موضعًا بعلامة `$` ناقصة من القالب النصي — **تُطبع للزائر حرفيًا**

نمط متكرر: `` `نص {STORE.shortName}` `` بدل `` `نص ${STORE.shortName}` ``، أو `"{STORE.name}"` بين علامتَي اقتباس عاديتين بدل مرجع فعلي.

| الملف | الأثر |
|---|---|
| `lib/seo.jsx` | `SITE.name` و`SITE.shortName` — **يظهر في `<title>` و`og:title` لكل صفحة منتج وتصنيف** |
| `app/layout.jsx` | الاسم الافتراضي للموقع كله |
| `app/(site)/shop/page.jsx` | العنوان الفرعي |
| `app/(site)/offers/page.jsx` | عنوان الصفحة |
| `app/(site)/about/page.jsx` | العنوان + وصف الميتا |
| `app/(site)/privacy/page.jsx` | الفقرة الافتتاحية |
| `app/(site)/contact/page.jsx` | رسالة واتساب |
| `app/feed.xml/route.js` | **`<g:brand>` — العلامة التجارية في كتالوج Google/Meta** |
| `app/feed.csv/route.js` | عمود العلامة التجارية |
| `components/site/StoreLogo.jsx` | الاسم والوصف الافتراضيان في الهيدر |
| `components/site/ContactForm.jsx` | رسالة واتساب |
| `components/site/MaintenanceCTA.jsx` | رسالة واتساب |

أخطرها `feed.xml`: كان يرسل النص `{STORE.shortName}` كعلامة تجارية لكل منتج في كتالوج Merchant Center.

## ٣.٤ `themeColor` مثبّت على ثيم واحد

`app/layout.jsx` كان يعلن `themeColor: "#0C1C77"` — أزرق ثيم `aqua` — بغض النظر عن الثيم النشط. صار `themeColors().primary`.

## ٣.٥ بادئة رقم الطلب `AR-` مثبّتة

`lib/db.sqlite.js` و`lib/db.pg.js` كانتا تولّدان `AR-YYMM-1001` في كل متجر — `AR` من «أريج النقاء». أُضيف `STORE.orderPrefix` إلى `store.config.js` (`"TB"` هنا)، والدالتان تقرآنه.

## ٣.٦ نصوص مجال المياه مكتوبة داخل `app/` و`components/site/`

الثغرة الأكبر معماريًا: نصوص خاصة بمتجر أجهزة تنقية كانت مكتوبة داخل ملفات الصفحات، **خارج `config/`** — فتتسرّب إلى أي متجر جديد مهما عُدّلت التهيئة.

**لم تُستبدل بنصوص ورد داخل النواة.** نُقلت إلى `content.config.js` كثوابت، وصارت ملفات النواة تقرأ منها:

| الثابت الجديد | يغذّي |
|---|---|
| `HERO_FALLBACK` | `HeroBanners → DefaultHero` («نقاء متجدد في كل قطرة») |
| `SHOP_PAGE` | عنوان `/shop` ووصفه وشريط الدعوة («نرشّح لك الجهاز المناسب») |
| `CATEGORY_PAGE` | شريط دعوة صفحة التصنيف («فريقنا الفني يجيبك») |
| `PRODUCT_PAGE` | أسئلة المنتج · الشارات السريعة · تبويب الضمان · شريط الدعوة |
| `ABOUT_PAGE` | عنوان «من نحن» («مياه نقية ليست رفاهية») |
| `CONTACT_FORM` | خيارات نموذج التواصل («حجز تركيب» · «طلب صيانة») |
| `CARD_LABELS` | شارات بطاقة المنتج («تركيب مجاني») |
| `PRIVACY_SECTIONS` | أقسام سياسة الخصوصية |

الشارات السريعة في صفحة المنتج صارت مدفوعة بالبيانات: كل شارة لها `when` (`always` · `freeShipping` · `freeInstall` · `priced`) يحدّد شرط ظهورها، بدل شروط مكتوبة في الكود.

**النتيجة المقيسة:** فحص ١٥ صفحة مُصيَّرة (الرئيسية · المتجر · العروض · من نحن · الخصوصية · التواصل · صفحة منتج · ٨ صفحات تصنيف) بحثًا عن ١٥ نمط تسرّب → **صفر تسرّب**.

## ٣.٧ ما لم يُصلَح عمدًا

`components/site/HowItWorks.jsx` يحمل نصًّا كاملًا عن تركيب أجهزة المياه، لكنه **غير مستورد في أي مكان** — لا في السجل ولا في أي صفحة. تركته كما هو؛ قسم `orderSteps` الجديد يؤدي دوره ويقرأ من التهيئة. احذفه من النواة أو حوّله للقراءة من `ORDER_STEPS`.

`components/site/UrgentForm.jsx` و`TechnicianForm.jsx` مليئان بنصوص المياه، لكنهما يظهران فقط حين تكون وحدة `maintenance` مفعّلة — وهي مطفأة هنا.

# ٤. ما عُدّل — بنية المشروع

## `package.json`

```diff
- "name": "areej-alnaqaa-admin"
+ "name": "tulip-bloom-store"
+ "seed:tulip": "NODE_OPTIONS=--experimental-sqlite node scripts/seed-tulip.mjs"
```

`scripts/seed.mjs` الأصلي و`npm run seed` بقيا كما هما.

## `public/images/` — حُذفت ١٠ صور

`prod_1.jpg` … `prod_10.jpg` — صور فلاتر مياه ومحطات تحلية متبقّية من متجر أريج النقاء. لا يشير إليها أي كود، وحجمها ٣٨٨ ك.ب من الوزن الميت.

---

# ٥. ما أُضيف

## `config/catalog.config.js`

٨ تصنيفات · **٢٥ منتجًا (٣ على الأقل لكل تصنيف)** · ٣ بنرات.

| التصنيف | السلَغ | الأيقونة |
|---|---|---|
| باقات الورد | `bouquets` | Sparkles |
| صناديق ورد | `flower-boxes` | Package |
| تنسيقات فازات | `vase-arrangements` | Droplet |
| ورد المناسبات | `occasions` | Award |
| هدايا وشوكولاتة | `gift-sets` | Coffee |
| نباتات داخلية | `plants` | Home |
| عطور وشموع | `candles-scents` | Flame |
| إضافات وبطاقات | `addons` | Tag |

المنتجات (٨٩ – ٧٥٠ ر.س) تغطي كل حالات العرض: خصم · شارة ترويجية · شارة رواج · شارة حالة · كمية محدودة · نفاد · حجز مسبق · صفقة صدارة · تحت حد الشحن المجاني وفوقه.

### ثلاثة قرارات في بيانات المنتجات

**`brand` = نوع الزهرة.** النواة تعرض الحقل كـ«الماركة» في فلتر المتجر. لا ماركات في متجر ورد، فاستُخدم لنوع الزهرة (ورد جوري · توليب · هيدرانجيا · أقحوان · نباتات داخلية · شموع) — أنفع فلتر ممكن هنا، بلا أي تعديل في المخطط.

**`freeInstall = false` في كل المنتجات.** تفعيله يُظهر شارة «تركيب مجاني» المدفونة في `ProductCard` و`OfferSpotlight`، وهي نصّ من مجال آخر.

**`rating = null` في كل المنتجات.** لا نجوم بلا تقييمات حقيقية موثّقة.

## `scripts/seed-tulip.mjs`

```bash
npm run seed:tulip              # بذر
npm run seed:tulip -- --reset   # حذف كل البيانات
```

يعمل على SQLite وPostgres. فرقان عن سكربت النواة:

1. **يمرّر `brand` و`stock` و`sortOrder` و`featuredOffer`** — سكربت النواة يُسقطها، فتضيع حالات النفاد والحجز المسبق كلها.
2. **يبذر البنرات بـSQL خام** لا عبر `createBanner` — التفاف حول الخلل ٣.٢ يبقى صالحًا حتى لو رفضت الإصلاح.

## `database/seed.tulipbloom.sql`

بيانات البذر بصيغة Postgres جاهزة للصق في Supabase SQL Editor. المعرّفات مسبوقة بـ`tbcat_` و`tbp_` و`tbb_`.

## `database/reset.tulipbloom.sql`

يحذف بيانات البذر وحدها ولا يمسّ ما أُضيف من لوحة التحكم.

---

# ٦. قرار المقاسات — لم يُضف حقل `sizes`

**القرار: لا عمود `sizes` في المخطط.**

الإضافة كانت ستتطلب لمس سبعة ملفات نواة: `lib/db.pg.js` (الترحيلة + رفع `SCHEMA_VERSION` + `LIST_COLS`) · `lib/db.sqlite.js` · `components/ProductForm.jsx` · `components/site/ProductActions.jsx` · `context/CartContext.jsx` (السعر يتغيّر بالمقاس) · `app/feed.xml/route.js`. هذا يكسر وعد «نواة واحدة، متاجر كثيرة»: كل متجر لاحق يرث تعديلًا لا يخصّه.

**ما نُفّذ بدلًا منه:**

| الآلية | أين |
|---|---|
| كل مقاس منتج مستقل بسعره | «روز كلاسيك — وسط» ٣٢٠ · «— كبير» ٥٢٠ |
| تفاصيل المقاس في جدول المواصفات | سطور `المقاس:` و`عدد الأزهار:` و`الارتفاع التقريبي:` في `full` — `parseSpecs` يحوّلها تلقائيًا |
| جدول مقارنة المقاسات | قسم `sizeGuide` في الرئيسية و`SHOP_SECTIONS` |

هذا أيضًا ما يفضّله Google Merchant Center: كل SKU بسعره وصورته ومخزونه، لا متغيّرات داخل منتج واحد.

**لو أردت مقاسات حقيقية داخل بطاقة المنتج لاحقًا**، فالمكان الصحيح ترقية النواة إلى v1.1 — لا ترقيع هذا المتجر.

---

# ٧. ملاحظات على النواة — لم تُعالَج

## ٧.١ ✅ نصوص مجال المياه — **عولجت**

كانت أحد عشر موضعًا في `app/` و`components/site/`. نُقلت كلها إلى `content.config.js` — انظر §٣.٦. فحص ١٥ صفحة مُصيَّرة: صفر تسرّب.

## ٧.٢ `lg:grid-cols-${cols}` صنف ديناميكي بلا `safelist`

`registry.jsx` يبني الصنف بقالب نصي، و`tailwind.config.js` بلا `safelist`. القيم `4` و`5` و`6` تعمل بالصدفة لأنها مكتوبة حرفيًا في مكوّنات أخرى (`TrustStrip` · `analytics/page`)؛ أي قيمة غيرها تفشل صامتة. **استُخدم `columns: 4`.**

## ٧.٣ `SHOP_SECTIONS` و`CATEGORY_SECTIONS` تهيئة ميتة

`app/(site)/shop/page.jsx` و`app/(site)/category/[slug]/page.jsx` يكتبان الأقسام مباشرة ولا يستوردان `SectionRenderer` إطلاقًا. `HOME_SECTIONS` وحدها تُستهلك فعليًا.

`sizeGuide` أُضيف إلى `SHOP_SECTIONS` استعدادًا لليوم الذي تُوصَّل فيه الصفحتان بالمحرّك، لكنه **لا يظهر في `/shop` اليوم**.

## ٧.٤ مسار قاعدة SQLite مكتوب حرفيًا

`lib/db.sqlite.js` يستخدم `data/areej.db` بغض النظر عن المتجر. غير ضار، لكنه محيّر عند تشغيل عدة متاجر.

## ٧.٥ `package.json` باسم متجر سابق

كان `"name": "areej-alnaqaa-admin"` في النواة المجرّدة.

---

# ٨. الحالة الحالية والخطوات التالية

## ✅ منجَز ومُختبَر

| | |
|---|---|
| البناء | `npm run build` — ٣٥ صفحة، صفر أخطاء |
| المسارات | `/` `/shop` `/offers` `/faq` `/about` `/contact` `/privacy` → 200 |
| السيو | `/sitemap.xml` `/robots.txt` `/feed.xml` → 200 |
| الحماية | `/admin` بلا جلسة → 307 |
| الرئيسية | الهيرو والمناسبات والتصنيفات والمنتجات ودليل المقاسات والعروض وخطوات الطلب — كلها تُعرض |
| التصنيفات | ٨ صفحات تصنيف → 200، كل واحدة بـ٣ منتجات أو أكثر |
| صفحة المنتج | 200 · المواصفات والتبويبات والمنتجات المشابهة |
| العروض | ٤ منتجات مخفَّضة تظهر |
| حالات المخزون | كمية محدودة · حجز مسبق · غير متوفر — كلها تُعرض |
| الطلبات | `POST /api/orders` → 201 · رقم `TB-2608-1003` |
| لوحة التحكم | تسجيل دخول 200 · المنتجات والطلبات والبنرات 200 |
| إنشاء بنر | `POST /api/banners` → 201 (كان 500 قبل الإصلاح) |
| تسرّب النصوص | ١٥ صفحة × ١٥ نمط → **صفر** |
| واتساب | كل الروابط على `966500000000` |

## ⚠️ يحتاج قرارك

**رفع إصلاحات النواة (§٣) إلى `store-core`.** تسعة عشر ملفًا. بدونها يرث كل متجر قادم: فشل بناء عند وجود بنر، و500 في `/admin/orders`، وبنرات لا تُنشأ، و`{STORE.shortName}` حرفيًا في عنوان كل صفحة وفي كتالوج Merchant Center، وبادئة طلب `AR-`، ونصوص أجهزة تنقية المياه.

## ⏳ قبل الإطلاق

| # | المهمة |
|---|---|
| ١ | رقم واتساب وهاتف حقيقيان (`config/store.config.js`) |
| ٢ | العنوان الفعلي وإحداثيات المعرض |
| ٣ | مشروع Supabase ← `database/schema.sql` ← `database/seed.tulipbloom.sql` |
| ٤ | `.env.local` — `DATABASE_URL` منفذ **6543** · `SESSION_SECRET` · بيانات اللوحة |
| ٥ | Vercel Blob **Public** ← رفع صور المنتجات. **`/feed.xml` فارغ حتى تُرفع الصور** — وهذا سلوك صحيح: Merchant Center يرفض المنتج بلا صورة، فالملف يستبعده بدل أن يُرفض كله |
| ٦ | `/admin/legal` — الشروط والاسترجاع والشحن. **Google Ads وMeta يرفضان المتجر بدونها** |
| ٧ | معايرة `LEAD_VALUES` في `lib/analytics.js` لمتوسط قيمة طلب الورد |
| ٨ | GTM من `/admin/settings` — واترك GA4 فارغًا |

## تشغيل محلي

```bash
npm install
npm run seed:tulip
npm run dev
```

بلا `DATABASE_URL` يسقط التطبيق تلقائيًا إلى SQLite في `data/areej.db`.

---

# ٩. مشروع Supabase — جاهز

| | |
|---|---|
| الاسم | **Tulip Bloom** |
| المرجع (ref) | `aepdabfzzgihmasexkdu` |
| المنطقة | `eu-central-1` (فرانكفورت) |
| الخطة | Free — **$0 شهريًا** |
| API URL | `https://aepdabfzzgihmasexkdu.supabase.co` |

**اختيار المنطقة:** فرانكفورت أقرب منطقة AWS ذات ارتباط جيد بالخليج (البحرين `me-south-1` غير متاحة في Supabase). المهم أن تضبط منطقة دوال Vercel على `fra1` أيضًا — إن بقيت على الافتراضي `iad1` (واشنطن) فكل استعلام يقطع المحيط الأطلسي ذهابًا وإيابًا.

> ⚠️ مشروع **Areej admin** الإنتاجي لم يُلمس. المشروع الثالث المتوقف (`pquyemgqqdepzykbsfmv`) تُرك كما هو.

## ما طُبِّق

| # | الترحيلة | المحتوى |
|---|---|---|
| ١ | `store_core_tables_and_indexes` | ٧ جداول · ١٣ فهرسًا · ٥ قيود · تسلسل `order_number_seq` |
| ٢ | `store_core_rls_and_privileges` | RLS على السبعة · سحب صلاحيات `anon`/`authenticated` · `ALTER DEFAULT PRIVILEGES` · ٣ صفحات قانونية · `schema_version = 7` |
| ٣ | البيانات | ٨ تصنيفات · ٢٥ منتجًا · ٣ بنرات · ٥ مفاتيح هوية في `settings` |

## التحقق

```
الجدول        rls    force
banners       true   false
categories    true   false
legal_pages   true   false
orders        true   false
products      true   false
settings      true   false
visits        true   false
```

مطابق للمواصفة. `get_advisors` يُعيد ٧ تنبيهات `INFO` بعنوان «RLS enabled, no policies» — **صحيحة ومقصودة**. لا تُضِف سياسات لتسكيتها: غياب السياسة هو المنع الكامل، والتطبيق يتصل بدور `postgres` (مالك الجداول) الذي يتجاوز RLS ما دام `FORCE` معطّلًا.

## توزيع الكتالوج

| التصنيف | المنتجات | النطاق السعري |
|---|---|---|
| باقات الورد | ٤ | ١٦٥ – ٥٢٠ |
| صناديق ورد | ٣ | ٢٤٠ – ٦١٠ |
| تنسيقات فازات | ٣ | ١٩٥ – ٤٤٥ |
| ورد المناسبات | ٣ | ٣٩٥ – ٧٥٠ |
| هدايا وشوكولاتة | ٣ | ٣٤٠ – ٦٩٠ |
| نباتات داخلية | ٣ | ٢٢٥ – ٤٢٠ |
| عطور وشموع | ٣ | ١٤٥ – ٢٦٥ |
| إضافات وبطاقات | ٣ | ٨٩ – ١٢٠ |

## ما تبقّى عليك

**كلمة مرور القاعدة** هي الشيء الوحيد الذي لا أستطيع قراءته — تُعرض مرة واحدة عند الإنشاء فقط:

`Supabase ← Project Settings ← Database ← Reset database password`

ولّد كلمة **بحروف وأرقام و`_` و`-` فقط** — الرموز `@ # / :` تكسر رابط الاتصال. ثم ضعها في `DATABASE_URL` مكان `[YOUR-PASSWORD]` في `.env.local`.

انسخ `.env.local.example` إلى `.env.local` — فيه `SESSION_SECRET` و`ADMIN_PASSWORD` مولَّدان بالفعل.

---

# ١٠. إعادة التصميم — من «نفس المتجر بألوان مختلفة» إلى هوية مستقلة

## ١٠.١ السببان الجذريان

التشابه لم يكن في المحتوى ولا الألوان، بل في عيبين بنيويين في النواة:

**أ) `themeCssVars()` معرّفة ولا تُستدعى.** الدالة موجودة في `theme.config.js` منذ البداية، لكن لا سطر في المشروع يستدعيها. النتيجة أن `globals.css` بقي يحمل `--navy: #0C1C77` و`--teal: #00C6C7` حرفيًا، فشريط التمرير وحلقة التركيز وظلّ البطاقات وتظليل النص وهيكل التحميل كلها زرقاء أريج مهما غُيّر الثيم.

**الإصلاح:** حقن `themeCssVars()` في `<head>` من `app/layout.jsx`، وإعادة كتابة `globals.css` ليستهلك المتغيّرات. الأسماء القديمة (`--navy` · `--teal` · `--line`) بقيت مربوطة بالجديدة فلا ينكسر أي مكوّن يستعملها.

**ب) خط واحد لكل شيء.** `TYPOGRAPHY.fontFamily` واحد يخدم العناوين والنصوص معًا — صفر تباين طباعي، وهو أكبر سبب في تشابه كل المتاجر المبنية على النواة.

**الإصلاح:** `headingFontFamily` كمفتاح جديد، وخطّان يُحمَّلان من `TYPOGRAPHY.googleFontUrl` بدل رابط مثبّت في `layout.jsx`.

## ١٠.٢ الطباعة

| | |
|---|---|
| العناوين | **El Messiri** 600 — إنساني بدفء خطّي، طابع بوتيك بلا زخرفة ثقيلة |
| النصوص | **Almarai** — هندسي نظيف، عالي الوضوح في البطاقات والجداول |
| الأرقام | صنف `.num` يُرجعها إلى Almarai مع `tnum` — أرقام El Messiri ضعيفة |

**تصحيح مهم:** النواة كانت تضع `letter-spacing: -0.01em` على كل العناوين. التتبّع السالب يضرّ العربية لأن الحروف متصلة، وتقريبها يُلصق الأسنان ببعضها. صار `--ls-heading: 0`.

كذلك: `headingWeight` من 800 إلى 600، `--sp-section` أوسع، `.h-display` أكبر (حتى 3.9rem)، و`.eyebrow` صار حروفًا كبيرة متباعدة تسبقها شرطة قصيرة — إشارة تحريرية.

## ١٠.٣ لوحة التحكم

**`app/admin/layout.jsx` كان فيه كائن ألوان مكتوب يدويًا** يتجاهل الثيم تمامًا. مثله في `app/admin/page.jsx`. كلاهما صار يشتق من `themeColors()`.

تغييرات بصرية: شعار بأيقونة زهرة وخط العناوين · حالة نشطة بشريط جانبي رفيع وخلفية `softTint` بدل التعبئة الكاملة · وزن أخف للعناصر غير النشطة.

**بطاقات إحصاء خاصة بالورود** — كلها محسوبة من بيانات موجودة، **بلا أي تعديل في المخطط**:

| المجموعة | المحتوى |
|---|---|
| اليوم | طلبات اليوم · طلبات جديدة لم تُعالج · متوسط قيمة الطلب · عروض نشطة |
| يحتاج انتباهك | نفد المخزون · كمية محدودة · حجز مسبق · منتجات بلا صورة |
| الإجماليات | المنتجات · التصنيفات · الطلبات · زوار ٧ أيام |

قسم «يحتاج انتباهك» يظهر فقط عند وجود سبب. منطقه أن الورد سلعة سريعة التلف — حالة المخزون أهم يوميًا من أي رقم تراكمي. وتنبيه «بلا صورة» يشرح الأثر مباشرة: هذه المنتجات تُستبعد من كتالوج Google وMeta تلقائيًا.

## ١٠.٤ أقسام جديدة — خمسة، إضافة فقط

ملف جديد `components/sections/TulipSections.jsx` للمكوّنات التفاعلية (مكوّنات عميل)، مربوط عبر `registry.jsx`. لم يُمسّ أي قسم من الأحد عشر الأصلية.

| القسم | الغرض |
|---|---|
| `editorialHero` | بديل شريط البنرات: طباعة كبيرة + ثلاث بلاطات غير متساوية الارتفاع (الأولى تمتد صفّين). كسر التناظر هو ما يفصل المتجر عن قالبه |
| `deliveryCountdown` | عدّاد حيّ لموعد قطع التوصيل. **صادق لا مصطنع:** يتحوّل تلقائيًا إلى «التسليم غدًا» بعد ٦ مساءً بدل أن يكذب |
| `giftFinder` | ثلاثة أسئلة → رسالة واتساب مُعبّأة. أكبر سبب لمغادرة متجر ورد هو التردّد لا السعر |
| `careGuide` | تبويبات العناية. يقلّل شكوى «الورد ذبل بسرعة» — أكثر شكوى في المجال وأضرّها على التقييمات |
| `lookbook` | شبكة تحريرية: أول منتج يمتد عمودين. بلا أي تغيير في `ProductCard` |

`lookbook` يستعمل `pickProducts` نفسها التي يستعملها `productGrid` — لا مصدر بيانات موازٍ.

**ترتيب الرئيسية الجديد:**

```
editorialHero → deliveryCountdown → occasions → lookbook → categories
→ giftFinder → sizeGuide → offers → orderSteps → careGuide
→ whyUs → trust → faq → cta
```

العدّاد تحت الهيرو مباشرة لأن الاستعجال يعمل وقت وصول الانتباه لا بعد التمرير. ومرشّح الهدايا في منتصف الصفحة حيث يبلغ التردّد ذروته.

## ١٠.٥ شريط الطلب اللاصق

`components/site/MobileOrderDock.jsx` — ملف جديد، يظهر على الجوال بعد تمرير ٤٠٠ بكسل. صفحة المنتج طويلة (مواصفات + تبويبات + مشابهة)، وزر الشراء يختفي بعد أول تمرير.

**لا يعدّل `ProductActions` ولا `CartContext`** — يستدعي `addToCart` نفسها.

**ملاحظة:** لم أضف زر واتساب ثالثًا إلى `ProductCard`. البطاقة فيها زرّان بالفعل، والثالث يزدحم على الجوال ويخفض معدّل النقر بدل أن يرفعه.

## ١٠.٦ الاختبار

| | |
|---|---|
| البناء | ٣٥ صفحة · صفر أخطاء |
| الواجهة | `/` `/shop` `/offers` `/faq` `/about` `/contact` `/privacy` → 200 |
| التصنيفات | ٨ صفحات → 200 |
| **الطلبات** | `POST /api/orders` → `TB-2608-1001` · جديد · 640 ر.س ✅ |
| **اللوحة** | ٩ صفحات → 200 (نظرة عامة · منتجات · تصنيفات · بنرات · عروض · طلبات · تحليلات · قانونية · إعدادات) |
| الحماية | `/admin` بلا جلسة → 307 · `/api/products` → 401 |
| تسرّب النصوص | صفر |

> `/cart` يُرجع 404 — **ليس انحدارًا**. النواة لا تحتوي مسار سلة أصلًا؛ السلة درج جانبي.

## ١٠.٧ ما لم يُمسّ

`database/schema.sql` · قاعدة Supabase · منطق `/api/*` · `lib/auth.js` · استعلامات `lib/db.*.js` · `context/CartContext.jsx` · `middleware.js` · `ProductCard` · `ProductActions` · `BuyNowButton` · الأقسام الأحد عشر الأصلية في السجل.

---

# ١١. التنقّل والصفحات ومكوّنات الواجهة

## ١١.١ ⚠️ الخلل الذي كشفته الصور — `MODULES` مُتجاهَل تمامًا

`MODULES.maintenance = false` مضبوط منذ المرحلة الأولى، ومع ذلك ظلّت «الصيانة الدورية» و«طلب فني» و«صيانة عاجلة» تظهر في الهيدر والفوتر.

**السبب:** `Header.jsx` و`Footer.jsx` يكتبان روابط التنقّل **حرفيًا داخل الملف** ولا يقرآن `MODULES` إطلاقًا. الهيدر كان يحوي أيضًا مصفوفة `NAV_LINKS` معرّفة **ولا تُستعمل** — الروابط الفعلية مكرّرة يدويًا في ثلاثة مواضع (سطح المكتب، الجوال، الفوتر).

**النتيجة العملية:** إطفاء أي وحدة لا يُخفي روابطها. أي متجر جديد يرث روابط صيانة أجهزة مياه في شريطه الرئيسي.

**الإصلاح:** `NAV_LINKS` صار ثابتًا في `content.config.js` — مصدر واحد للهيدر والفوتر. كل رابط يحمل `module` اختياريًا يُخفيه تلقائيًا عند الإطفاء، و`accent: true` يعطيه شكل شارة بارزة.

## ١١.٢ ثلاث صفحات بديلة لصفحات الصيانة

بدل إخفاء الروابط وترك فراغ، أُضيفت ثلاث صفحات من صميم المجال:

| المسار | المحتوى |
|---|---|
| `/occasions` | شبكة ٦ مناسبات (كل واحدة تفتح واتساب برسالة مُعبّأة) + جدول «المقاس يتبع المناسبة» + مقترحات |
| `/care` | أربع خطوات عناية تفصيلية + **خمسة أخطاء شائعة** في أكورديون. الأخطاء أوضح أثرًا من النصائح |
| `/gift-finder` | مرشّح الهدايا كصفحة مستقلة + مقترحات متوفّرة |

الشريط صار: الرئيسية · التشكيلة ▾ · المناسبات · العروض · دليل العناية · نبذة عنا · تواصل معنا · **رشّح لي هدية** (شارة بارزة، بديل شارة «صيانة عاجلة» الحمراء).

صفحات `/maintenance/*` بقيت في الشيفرة بلا روابط — لم أحذفها لأنها جزء من النواة، وحذفها يجعل الحزمة غير قابلة للمزامنة مع التحديثات القادمة.

## ١١.٣ الهيدر — أُعيد بناؤه

شريط وعد رفيع بلون الهوية فوق (توصيل نفس اليوم · شحن مجاني فوق ٣٠٠) · صفّ هوية بارتفاع أكبر · **قائمة التشكيلة المنسدلة صارت عمودين** بدل قائمة عمودية طويلة · روابط بوزن ٥٠٠ لا ٧٠٠ (الوزن الثقيل على كل رابط يُلغي التسلسل البصري) · قائمة جوال بمجموعات معنونة بدل قائمة مسطّحة.

## ١١.٤ `PageHero` — أُزيلت شارة الأيقونة

كانت تعرض مربّعًا بارزًا بأيقونة التصنيف فوق كل ترويسة. في صورتك ظهرت **قطرة ماء فوق «نبذة عن توليب بلوم»** — أيقونة `Droplet` الموروثة من متجر المياه.

الآن: سطر تحريري (`eyebrow`) بحروف متباعدة تسبقه شرطة قصيرة، والأيقونة عنصر رفيع بجانبه لا مربّع. وأُضيف تدرّج ثانٍ بلون `soft` لعمق أكبر.

## ١١.٥ `ProductBrowser` — الفلتر والبحث

| قبل | بعد |
|---|---|
| شرائح كبسولية بلون كل تصنيف | مستطيلات ناعمة بحد شعري، اللون في الأيقونة فقط |
| «الماركة» | **«نوع الزهرة»** — لا ماركات في متجر ورد |
| «ابحث بالاسم أو الوصف أو الماركة…» | «ابحث عن باقة، مناسبة، أو نوع زهرة…» |
| «فلترة» · «الأكثر ملاءمة» | «تصفية» · «المختارة لك» |
| عناوين الفلاتر بخط عريض داكن | حروف صغيرة متباعدة بلون هادئ |
| لوحة فلاتر بيضاء بظل | خلفية `pearl` بحد شعري بلا ظل |

ثماني شرائح بثمانية ألوان صارخة تُنتج فوضى بصرية — اللون الآن لمسة في الأيقونة لا خلفية كاملة.

## ١١.٦ `CategoryCard` و`ContactForm`

**البطاقة:** حد شعري، هالة لونية تظهر عند المرور فقط، خط سفلي يحمل لون التصنيف مع «تصفّح القسم».

**النموذج:** ترويسة تحريرية («كيف نساعدك؟» بدل «أرسل لنا رسالة») · حقول بخلفية `pearl` وحد رفيع وتركيز بلون الهوية · تسميات بحروف صغيرة متباعدة · نص إرشادي يطلب المناسبة والميزانية والحي.

## ١١.٧ الاختبار

| | |
|---|---|
| البناء | **٣٨ صفحة** · صفر أخطاء |
| المسارات | ١١ مسارًا → 200 (منها `/occasions` `/care` `/gift-finder` الجديدة) |
| **الطلبات** | `POST /api/orders` → `TB-2608-1002` ✅ |
| **اللوحة** | تسجيل دخول + ٤ صفحات → 200 |
| تسرّب نصوص الصيانة | **صفر عبر ٧ صفحات** |

## ١١.٨ ما لم يُمسّ

`database/schema.sql` · Supabase · `/api/*` · `lib/auth.js` · `lib/db.*.js` · `CartContext` · `middleware.js` · `ProductCard` · `ProductActions` · `BuyNowButton` · منطق الفلترة والفرز في `ProductBrowser` (التصميم فقط تغيّر).

---

# ١٢. حسابات العملاء

## ١٢.١ القرار المعماري — لماذا لا Supabase Auth

النواة تعمل على SQLite محليًا وPostgres إنتاجًا. Supabase Auth يتطلب SDK، ويكسر التطوير المحلي، ويُنشئ نظام جلسات ثانيًا موازيًا لنظام المسؤول القائم. المصادقة الذاتية على نمط `lib/auth.js` تحافظ على المحرّكين وعلى نظام واحد.

| القرار | السبب |
|---|---|
| **scrypt** من `node:crypto` | مدمج، بلا اعتماد خارجي، مقاوم للذاكرة. `bcryptjs` الخالص بطيء بلا مقابل، و`bcrypt` الأصلي يحتاج بناءً محليًا يكسر النشر على Vercel |
| **جلسات في القاعدة** | الكوكي الموقّع وحده لا يُبطَل قبل انتهائه. التخزين يتيح: خروج من كل الأجهزة، وإبطال فوري عند الحظر أو تغيير كلمة المرور |
| **الرمز مجزّأ (SHA-256)** | الرمز الخام لا يُحفظ إطلاقًا — تسريب القاعدة لا يمنح جلسات |
| **الحماية في `layout.jsx` لا `middleware`** | الـmiddleware على Edge Runtime بلا وصول للقاعدة. الحماية في طبقة Node |

## ١٢.٢ الممارسات الأمنية المطبَّقة

| الممارسة | التطبيق |
|---|---|
| رسائل موحّدة | «البريد أو كلمة المرور غير صحيحة» لكل حالات الفشل — يمنع **تعداد الحسابات** |
| تجزئة وهمية | عند بريد غير مسجّل نصرف زمن scrypt مماثلًا — بدونه يكشف **فرق التوقيت** أي بريد مسجّل |
| تحديد المحاولات | ٦ محاولات ثم قفل ١٥ دقيقة |
| كوكي | `httpOnly` + `SameSite=Lax` + `Secure` في الإنتاج |
| تطبيع البريد | قصّ ومسافات وحالة أحرف — يمنع تسجيل نفس العنوان مرتين |
| تغيير كلمة المرور | يتطلب الحالية، ويُبطل **كل** الجلسات |
| الحظر | يحذف جلسات العميل فورًا |
| قيود الملكية | كل حذف مقيّد بـ`customerId` — بدونه يحذف عميل بيانات غيره |
| `publicCustomer()` | تنقية إلزامية قبل أي تمرير للمتصفح. **فُحص: صفر ظهور لـ`passwordHash` في HTML الواجهة واللوحة** |

**سياسة كلمة المرور:** ٨ أحرف حدًّا أدنى + منع القائمة الشائعة. لا فرض رموز وأرقام — يدفع الناس إلى أنماط متوقّعة مثل `Password1!` وهي أضعف من عبارة طويلة بسيطة.

## ١٢.٣ قاعدة البيانات — ترحيلة v8

`SCHEMA_VERSION` من `7` إلى `8`. **جداول جديدة فقط**، ولا تُمسّ الجداول القائمة عدا عمود واحد.

| الجدول | الغرض |
|---|---|
| `customers` | الحساب، التجزئة، الحالة، عدّاد المحاولات |
| `customer_sessions` | جلسات مجزّأة قابلة للإبطال |
| `customer_addresses` | عناوين التسليم المحفوظة |
| `customer_recipients` | **دفتر المستلمين** — من نرسل له لا من يشتري |
| `customer_reminders` | تذكيرات المناسبات (شهر/يوم، تتكرّر سنويًا) |
| `password_resets` | رموز مجزّأة صالحة مرة واحدة |

`orders.customerId` عمود **قابل للإفراغ**. الطلبات القديمة والطلبات كضيف تبقى صالحة — **فُحص: طلب الضيف يُرجع 201 كما كان**.

> التذكيرات تحفظ الشهر واليوم فقط. حفظ سنة الميلاد يجعل التذكير يمرّ مرة واحدة، وحساب «القادم» عبر حدّ السنة (ديسمبر ← يناير) يُنتج استعلامًا مختلفًا بين المحرّكين — لذلك يُحسب في JavaScript.

## ١٢.٤ ما بُني

**الواجهة:** `/account/login` · `/register` · `/account` (نظرة عامة بالمناسبات القادمة) · `/orders` (مع **إعادة الطلب**) · `/recipients` (المستلمون + التذكيرات) · `/addresses` · `/profile`

**API:** `register` · `login` · `logout` · `profile` · `addresses` · `recipients` · `reminders`

**اللوحة:** `/admin/customers` — بحث، إحصاءات (الحسابات · المتكرّرون · قيمة الشراء · الموقوفون)، طلبات وقيمة شراء لكل عميل، إيقاف/تفعيل، **توليد رابط إعادة تعيين**.

## ١٢.٥ ⚠️ البريد — الفجوة المتبقية

**لا مزوّد بريد في المشروع.** لذلك:

| | الحالة |
|---|---|
| التسجيل والدخول والجلسات وسجل الطلبات | ✅ تعمل كاملة |
| تأكيد البريد | يُسجَّل `emailVerified = false` ولا يمنع الشراء — فرضه يقتل التحويل |
| استعادة كلمة المرور ذاتيًا | ❌ **معطّلة** |
| استعادة عبر اللوحة | ✅ زر يولّد رابطًا صالحًا ٦٠ دقيقة ومرة واحدة، تنسخه وترسله على واتساب |

**لتفعيل البريد:** اربط نطاقًا حقيقيًا بـVercel أولًا (لا يمكن توثيق `*.vercel.app` — لا تملك DNS)، ثم أنشئ حساب Resend وأضف `RESEND_API_KEY`. الخطة المجانية ٣٬٠٠٠ رسالة شهريًا بحد ١٠٠ يوميًا ونطاق واحد.

**ناقص بعد إضافة المزوّد:** صفحة `/account/reset` وطبقة الإرسال. الرموز والتخزين والتحقّق جاهزة بالفعل.

## ١٢.٦ الاختبار

| | |
|---|---|
| البناء | **٤٦ صفحة** · صفر أخطاء |
| صفحات الحساب بلا جلسة | ٥ صفحات → 307 تحويل · `/account/login` → 200 |
| دورة الحساب | تسجيل 201 · `/account` بجلسة 200 |
| تعداد الحسابات | تسجيل مكرّر → 409 برسالة عامة ✅ |
| كلمة مرور ضعيفة | → 400 ✅ |
| تطبيع البريد | `  KHALED@example.com  ` يسجّل الدخول ✅ |
| إبطال الجلسة | الرمز بعد الخروج → 401 ✅ |
| **ربط الطلب** | `TB-2608-1001` ظهر في `/account/orders` ✅ |
| **طلب الضيف** | → 201 (لم ينكسر) ✅ |
| اللوحة | `/admin/customers` + ٣ صفحات → 200 |
| تسريب التجزئة | **صفر** في الواجهة واللوحة ✅ |

## ١٢.٧ ما لم يُمسّ

`database/schema.sql` · `lib/auth.js` (مصادقة المسؤول منفصلة تمامًا) · `context/CartContext.jsx` · منطق `createOrder` · `ProductCard` · `ProductActions`.

**عُدّل:** `middleware.js` (سطران لفتح `/api/account/*` — كل مسار منها يتحقّق من جلسته بنفسه ويردّ 401)، و`app/api/orders/route.js` (ربط صامت لا يُفشل الطلب عند فشله)، و`lib/db.sqlite.js` و`lib/db.pg.js` (جداول v8 + تصدير `getDb`).

> ⚠️ **عند النشر:** الترحيلة تعمل تلقائيًا عند أول طلب — `ensureSchema` يقرأ `schema_version` ويجد `7` فينفّذ v8. لا حاجة لتشغيل SQL يدويًا على Supabase.

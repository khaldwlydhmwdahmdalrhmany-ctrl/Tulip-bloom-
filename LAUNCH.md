# دليل الإطلاق — توليب بلوم

## ١. قبل أي شيء

```bash
npm install
npm run build
```

## ٢. قاعدة البيانات

المخطّط **v18** يُطبَّق تلقائيًا عند أول طلب. لا تشغّل SQL يدويًا.

> ⚠️ مشروع Supabase عندك عليه **v7** فقط. عند أول تشغيل سيقفز إلى v18
> ويُنشئ ١٥ جدولًا جديدًا. الترحيلات كلها `IF NOT EXISTS` وآمنة،
> لكن **راقب أول طلب** وتحقّق من الجداول في Supabase بعده.

## ٣. متغيّرات البيئة

| المتغيّر | إلزامي |
|---|---|
| `DATABASE_URL` | ✅ منفذ **6543** (Transaction pooler) |
| `SESSION_SECRET` | ✅ الخادم يرفض العمل في الإنتاج بدونه |
| `ADMIN_USERNAME` · `ADMIN_PASSWORD` | ✅ |
| `NEXT_PUBLIC_SITE_URL` | ✅ الدومين الكامل — تعتمد عليه الخريطة والروابط وwebhooks |
| `BLOB_READ_WRITE_TOKEN` · `PUBLIC_BLOB_STORE_ID` | لرفع الصور (Blob **Public**) |

بعد إضافة أي متغيّر: **أعد النشر** — Vercel لا يطبّقها على نشر قائم.

## ٤. تهيئة الإطلاق

```bash
npm run setup:launch
```

يُنشئ: منطقتَي شحن بثلاثة أسعار · ثلاث نوافذ تسليم · تفعيل الدفع عند الاستلام والتحويل · محتوى الصفحات القانونية الثلاث. **آمن للتكرار.**

## ٥. ما يجب أن تكمله بنفسك

| # | المهمة | أين |
|---|---|---|
| ١ | **رقم واتساب وهاتف حقيقيان** — الحالي `966500000000` | `config/store.config.js` |
| ٢ | **العنوان وإحداثيات المعرض** — الحالية وسط الرياض | `config/store.config.js` |
| ٣ | **بيانات التحويل البنكي** — البنك والآيبان | `/admin/payments` |
| ٤ | **صور المنتجات** — كلها بلا صور، و`/feed.xml` فارغ حتى ترفعها | `/admin/products` |
| ٥ | **مراجعة النصوص القانونية** — أساس عملي يحتاج مطابقة سياستك | `/admin/legal` |
| ٦ | **معايرة `LEAD_VALUES`** لمتوسط قيمة طلب الورد | `lib/analytics.js` |
| ٧ | **GTM** من الإعدادات — واترك GA4 فارغًا | `/admin/settings` |

## ٦. تفعيل الدفع الإلكتروني

1. `/admin/payments` ← اختر البوابة ← ألصق المفاتيح ← فعّلها
2. انسخ **رابط الـwebhook** المعروض والصقه في لوحة البوابة
3. ابدأ بوضع **اختبار**، ثم بدّل إلى **مباشر**

> ⚠️ بوابة مفعّلة بلا سرّ webhook تُظهر تحذيرًا أحمر — بدونه تبقى كل
> العمليات «معلّقة» رغم دفع العملاء.

**ناقص:** استدعاءات API لكل بوابة. الواجهة موحّدة في `lib/gateways.js`
(`createCharge`) وتُرجع `not_implemented` حتى تُكتب. التحقّق من التوقيع
ومنع التكرار ومطابقة المبلغ **جاهزة ومُختبَرة**.

## ٧. تفعيل شركات الشحن

اليوم: أنشئ الشحنة في موقع الشركة، ألصق رقم البوليصة في `/admin/shipping`،
فيُولَّد رابط التتبّع تلقائيًا. **لا يحتاج أي مفتاح.**

الربط الآلي: نفس وضع البوابات — الواجهة في `lib/carriers.js` جاهزة.

## ٨. إعادة تعيين كلمة مرور عميل

لا مزوّد بريد بعد. الحل المؤقت: `/admin/customers` ← زر المفتاح ←
انسخ الرابط وأرسله على واتساب. صالح **٦٠ دقيقة ومرة واحدة**.

عند ربط نطاق حقيقي: أنشئ حساب Resend وأضف `RESEND_API_KEY`.

## ٩. فحص أخير قبل الإعلان

- [ ] طلب تجريبي كامل من `/checkout` — تحقّق أن الإجمالي = المنتجات − الخصم + الشحن
- [ ] `/admin/orders` يعرضه بالشحن والنافذة
- [ ] `/robots.txt` و`/sitemap.xml` بالدومين الصحيح
- [ ] `/admin/seo` ← الفحص ← عالج الحرجة
- [ ] Deployment Protection: `all_except_custom_domains`
- [ ] **غيّر `ADMIN_PASSWORD`**
